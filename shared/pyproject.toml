[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "shared"
version = "0.1.0"
description = "Biblioteca compartida para el proyecto de criptografía"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]